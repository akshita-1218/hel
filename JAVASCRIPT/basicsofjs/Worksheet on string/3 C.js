// 1. Template literals are enclosed by "backticks (``)" instead of quote

// 2. str.split(" ") will return an array of "words"

// 3. str.toLowerCase() is used to convert a string to "lowercase"

// 4. str.replaceAll("a", "z") replaces "all" occurrences of the letter "a"

// 5. str.startsWith("web") returns a "boolean" value